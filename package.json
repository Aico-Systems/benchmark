{
    "name": "@aico/benchmark",
    "version": "1.0.0",
    "type": "module",
    "description": "LLM Provider Benchmark Suite for AICO",
    "scripts": {
        "benchmark": "bun run src/index.ts",
        "build": "bun build src/index.ts --outdir dist --target node",
        "test": "bun test"
    },
    "dependencies": {
        "chalk": "^5.3.0",
        "commander": "^12.0.0"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "tsx": "^4.21.0",
        "typescript": "^5.0.0"
    }
}
